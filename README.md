# docker-selenium-behat
Install docker.
#  RUN:
    - docker-compose run -d chrome
    - docker-compose run composer
    - docker-compose build php
    - docker-compose run php
    - docker-compose down